import 'package:flutter/material.dart';

class FlashNotesApp extends StatelessWidget {
  const FlashNotesApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(title: 'Flash Notes');
  }
}
